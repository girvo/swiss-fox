{
  "name": "swiss-fox",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "simplifyify src/index.js --outfile build/app.js --minify",
    "watch": "simplifyify src/index.js --outfile build/app.js --watch --debug",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "babel-eslint": "^7.0.0",
    "standard": "^8.4.0"
  },
  "dependencies": {
    "bubleify": "^0.5.1",
    "koa": "^1.2.4",
    "koa-mount": "^1.3.0",
    "koa-static": "^2.0.0",
    "preact": "^6.3.0",
    "simplifyify": "^2.0.4"
  },
  "browserify": {
    "transform": [
      [
        "bubleify",
        {
          "jsx": "h"
        }
      ]
    ]
  }
}
